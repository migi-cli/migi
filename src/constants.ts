const LOW_NODE_VERSION = "v14.15.0";
const DEFAULT_ORG = "migi-template";
const DEFAULT_CLI_HOME = ".migi";
const DEFAULT_REGISTRY = "https://registry.npmjs.org";
const DEPENDENCIES_PATH = "dependencies";

export {
  LOW_NODE_VERSION,
  DEFAULT_ORG,
  DEFAULT_CLI_HOME,
  DEFAULT_REGISTRY,
  DEPENDENCIES_PATH,
};
